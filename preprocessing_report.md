# Preprocessing Report — `Survived` 이진 분류
*생성일시: 2026-05-29 06:22*

---
## 처리 요약
| 항목 | 내용 |
|---|---|
| 제거 컬럼 (결측률 >30%) | `['Cabin']` |
| 제거 컬럼 (ID) | `['PassengerId', 'Name']` |
| 수치형 피처 (median impute) | `['Pclass', 'Age', 'SibSp', 'Parch', 'Fare']` |
| 저카디널리티 (OHE) | `['Sex', 'Embarked']` |
| 고카디널리티 (Target Enc.) | `['Ticket']` |
| 클래스 불균형 비율 | 1.61:1 |
| SMOTE 적용 | 아니오 |

## 산출물
| 파일 | 설명 |
|---|---|
| `preprocessor.pkl` | sklearn ColumnTransformer (fit 완료) |
| `target_encoder.pkl` | CrossVal TargetEncoder (누수 방지) |
| `train_processed.csv` | 전처리된 훈련 데이터 |

## 다음 단계 (모델링) 주의사항
- `preprocessor.pkl`을 `joblib.load`해서 새 데이터에 `.transform()`만 사용
- 불균형 비율 1.6:1 → Accuracy 사용 가능
- Cross-validation 시 전처리(fit)를 fold 내부에서 수행하거나 이 파이프라인 재사용
- Target Encoding은 OOF 방식으로 누수 방지됨 — 별도 재학습 불필요

---
*Generated by Kaggle Preprocessing Agent*